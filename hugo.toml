# hugo.toml - Core configuration for the site builder
#
# Only add settings here that *never* changes.
#
# For site-specific settings, use hugo.site.toml.
# For variant-specific settings, use config.<variant>.toml.

languageCode = 'en-us'
theme = 'union'
disableKinds = ["RSS"]

# Enable sitemap generation
[outputs]
  # 'home': The main site homepage. 'HTML' for the page, 'RSS' for feed readers, 'SITEMAP' for search engine indexing.
  home = ["HTML", "RSS", "SITEMAP"]

  # 'page': Individual content pages. Only 'HTML' is needed for standard page rendering.
  page = ["HTML"]

  # 'section': Top-level content sections (e.g., docs, tutorials). 'HTML' for rendering, 'RSS' for section-specific feeds.
  section = ["HTML", "RSS"]

  # 'taxonomy': Used for tag/category listing pages. 'HTML' for rendering, 'RSS' for taxonomy feeds.
  taxonomy = ["HTML", "RSS"]

  # 'taxonomy': Used for taxonomy term pages (previously 'taxonomyTerm'). Only 'HTML' is needed for these listings.
  taxonomy = ["HTML"]

[params]
_merge = "deep"
show_inactive = false
highlight_active = false

[markup]
  [markup.goldmark]
    [markup.goldmark.parser]
      [markup.goldmark.parser.attribute]
        block = true

#----------------------------------------------------------
# Search

[params.search]
app_id = "ZK72K15QRI"
search_key = "59e5451a3db8540ee70dc8dd04fe6287"
index_name = "union"
