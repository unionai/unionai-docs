---
title: ResolvedIdentifiers
version: 1.16.14
variants: +flyte +byoc +selfmanaged +serverless
layout: py_api
---

# ResolvedIdentifiers

**Package:** `flytekit.remote.remote`

```python
class ResolvedIdentifiers(
    project: str,
    domain: str,
    name: str,
    version: str,
)
```
| Parameter | Type | Description |
|-|-|-|
| `project` | `str` | |
| `domain` | `str` | |
| `name` | `str` | |
| `version` | `str` | |

