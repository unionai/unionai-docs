---
title: VolumeMountError
version: 0.4.2
variants: +flyte +union
layout: py_api
---

# VolumeMountError

**Package:** `flyteplugins.union.errors`

The volume could not be mounted — the client exited prematurely, the
metadata store failed to start, or format failed.


## Parameters

```python
class VolumeMountError(
    message: str,
)
```
| Parameter | Type | Description |
|-|-|-|
| `message` | `str` | |

